{
  "_comment": "Paste the 'mcpServers' block into ~/Library/Application Support/Claude/claude_desktop_config.json, then fully quit and restart Claude Desktop.",
  "mcpServers": {
    "bachata-sync": {
      "command": "/path/to/bachata-beat-story-sync/venv/bin/python",
      "args": [
        "/path/to/bachata-beat-story-sync/mcp_server.py"
      ],
      "env": {
        "PYTHONUNBUFFERED": "1"
      }
    }
  }
}
